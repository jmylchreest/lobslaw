// Package mcp is an MCP (Model Context Protocol) client that
// consumes MCP servers declared by plugins. MCP tools appear in
// the tool registry and go through the same policy + hooks
// pipeline as native tools.
package mcp

// Package plugins implements the plugin install/enable/import
// lifecycle. Plugin directory format is Claude Code-compatible:
// plugin.toml + hooks/, skills/, commands/, agents/, .mcp.json.
package plugins

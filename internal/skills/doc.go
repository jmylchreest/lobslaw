// Package skills implements the skill registry and invoker.
// OpenClaw/clawhub-compatible manifest format. Trust model is
// trust-on-install-after-operator-review (SHA-pinned); optional
// minisign signatures via skills.require_signed.
package skills
